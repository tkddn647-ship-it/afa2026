# sever/env.ingest 로 복사 후 사용 (app.py 시작 시 자동 로드)
# cp env.ingest.example env.ingest

# 로깅 서버 포트
PORT=8000

# ingest 수신 데이터를 실시간 대시보드로 포워드
MONITOR_FORWARD_URL=http://127.0.0.1:8011/api/live/ingest
FORWARD_TIMEOUT_SEC=2.0

# 로그 저장 경로
LOG_DIR=logs
